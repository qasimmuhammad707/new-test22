<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cinematic Task Completion</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background: #050505;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
            font-family: 'Segoe UI', Roboto, sans-serif;
        }

        .scene {
            perspective: 1000px;
        }

        .text-wrapper {
            font-size: 5rem;
            font-weight: 900;
            color: #fff;
            text-transform: uppercase;
            letter-spacing: 8px;
            text-align: center;
            transform-style: preserve-3d;
            animation: cinematicRotate 10s infinite linear;
            text-shadow: 0 0 10px #00d2ff, 0 0 20px #00d2ff, 0 0 40px #00d2ff;
        }

        @keyframes cinematicRotate {
            0% {
                transform: rotateX(0deg) rotateY(0deg) scale(0.8);
                filter: hue-rotate(0deg);
            }
            25% {
                transform: rotateX(10deg) rotateY(90deg) scale(1.1);
            }
            50% {
                transform: rotateX(0deg) rotateY(180deg) scale(1.3);
                filter: hue-rotate(180deg);
            }
            75% {
                transform: rotateX(-10deg) rotateY(270deg) scale(1.1);
            }
            100% {
                transform: rotateX(0deg) rotateY(360deg) scale(0.8);
                filter: hue-rotate(360deg);
            }
        }

        /* Ambient background glow */
        .ambient-light {
            position: absolute;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at center, rgba(0, 210, 255, 0.15) 0%, transparent 70%);
            pointer-events: none;
        }
    </style>
</head>
<body>
    <div class="ambient-light"></div>
    <div class="scene">
        <div class="text-wrapper">
            Today Task<br>is Done
        </div>
    </div>
</body>
</html>